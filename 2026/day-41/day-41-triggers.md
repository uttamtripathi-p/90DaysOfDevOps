# What does fail-fast: true (the default) do vs false?
## fail-fast: true
### Doesn't execute another matrix if one of them fails
## fail-fast: false
### runs another matrix even when one fails

# What is the cron expression for every Monday at 9 AM?
## 0 9 * * 1
